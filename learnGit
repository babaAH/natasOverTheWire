mkdir git #создай папку с гитом 
git init
git config --global user.email "your@email"
git config --global user.name "yourSureName"

git remote add -f --tags natas https://github.com/babaAH/natasOverTheWire.git
git clone https://github.com/babaAH/natasOverTheWire.git

git add перечисли файлы через пробел

git commit -m "commit text"

git push -u origin master # или просто git push кинет в ветку по умолчанию 
